# Auto-activate the project virtual environment when entering this directory.
if [ -f .venv/bin/activate ]; then
  source .venv/bin/activate
fi

# Per-developer overrides (kept out of git).
source_env_if_exists .envrc.local
